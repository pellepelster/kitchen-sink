apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "testinfra.fullname" . }}-test-connection"
  labels:
    {{- include "testinfra.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "testinfra.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
